name: Publish Blog Posts

on:
  push:
    paths:
      - 'blog/source/**'
      - '.github/workflows/publish-blog.yml'

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install markdown

      - name: Run blog build
        run: |
          cd $GITHUB_WORKSPACE
          python3 build_blog.py

      - name: Regenerate sitemaps
        run: |
          cd $GITHUB_WORKSPACE
          python3 build_sitemap.py

      - name: Commit and push changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add blog/posts/ blog/index.html blog/feed.xml blog/search-index.json blog/popular-posts.json blog/tags/ blog/archive/ sitemap-blog.xml sitemap-tags.xml sitemap-index.xml
          git diff --quiet && git diff --staged --quiet || (
            git commit -m "Publish new blog post(s) from source markdown" &&
            git push
          )
